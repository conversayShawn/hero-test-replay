# name: Cypress Tests

# on: push

# env:
#   VITE_API_URL: http://localhost:3001
#   CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
#   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#   DEBUG: "cypress:*"

# jobs:
#   install:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       - name: Cypress install
#         uses: cypress-io/github-action@v5
#         with:
#           runTests: false
#           build: npm run build
#       - name: Save build folder
#         uses: actions/upload-artifact@v3
#         with:
#           name: build-folders
#           if-no-files-found: error
#           path: |
#             client/dist
#             server/dist

#   tests:
#     runs-on: ubuntu-latest
#     needs: install
#     strategy:
#       fail-fast: false
#       matrix:
#         browser: [chrome, firefox]
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3

#       - name: Download the build folders
#         uses: actions/download-artifact@v3
#         with:
#           name: build-folders

#       - name: Run End-to-End Tests on ${{ matrix.browser }}
#         uses: cypress-io/github-action@v5
#         with:
#           start: npm run start:ci
#           wait-on: 'http://localhost:3000, http://localhost:3001/heroes'
#           command: npm run test:e2e:ci
#           browser: ${{ matrix.browser }}

#       - name: Run Component Tests on ${{ matrix.browser }}
#         uses: cypress-io/github-action@v5
#         with:
#           command: npm run test:ct:ci
#           browser: ${{ matrix.browser }}

#       - name: Run API Tests on ${{ matrix.browser }}
#         uses: cypress-io/github-action@v5
#         with:
#           start: npm run start:ci
#           wait-on: 'http://localhost:3000, http://localhost:3001/heroes'
#           command: npm run test:api:ci
#           browser: ${{ matrix.browser }}
